# 临时 HTTP 配置 - 仅用于 Let's Encrypt 验证
server {
    listen 80;
    listen [::]:80;
    server_name test.kanashortplay.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}
