name: face_swap
description: AI换图 - 智能AI换脸应用
publish_to: 'none'
version: 1.5.6+44

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.9.2
  provider: ^6.1.0
  shared_preferences: ^2.2.0
  image_picker: ^1.0.0
  cached_network_image: ^3.3.0
  photo_view: ^0.15.0
  fluttertoast: ^8.2.0
  shimmer: ^3.0.0
  image_gallery_saver_plus: ^3.0.0
  path_provider: ^2.0.0
  video_player: ^2.2.0
  package_info_plus: ^5.0.0
  flutter_sticky_header: ^0.6.5
  share_plus: ^7.2.2
  # sign_in_with_apple: ^5.0.0  # TODO: enable after local Flutter SDK available

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
