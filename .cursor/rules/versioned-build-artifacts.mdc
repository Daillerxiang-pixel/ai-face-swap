---
description: 安装包输出目录（F:\work\And）与版本化命名；测试包 test、正式包 and
alwaysApply: true
---

# 安装包输出目录（用户本机，长期约定）

客户端需要打包给用户安装测试或正式发布时，**不要把产物只放在仓库默认的 `build/` 下就结束**：构建完成后须复制或输出到用户指定目录：

| 类型 | 目录（在 `F:\work\And\<项目名称>\` 下） |
|------|----------------------------------------|
| **测试包** | `F:\work\And\<项目名称>\test\` |
| **正式包** | `F:\work\And\<项目名称>\and\` |

- `<项目名称>`：与当前仓库对应目录名一致即可（本仓库示例：`ai-face-swap`）。
- 若 `F:\` 或目标路径不存在，构建前创建目录；完成后在回复里给出**完整文件路径**（含版本化文件名，见下文）。

## 版本化构建产物命名

### 原则

1. **凡对外分发、归档、上传 CDN/网盘的安装包与主要版本文件**（如 `.apk`、`.ipa`、`.aab`、发布用 `.zip`），**文件名必须包含版本号**，禁止长期使用无版本号的固定名（如单独的 `release.apk`）。
2. **版本号**以工程内权威来源为准并保持**自增长**：Flutter 使用 `pubspec.yaml` 的 `version:`（`x.y.z+build`）；Node 等使用 `package.json` 的 `version`。发新版前在对应文件中**递增**再构建。
3. **测试环境 / 测试服专用包**：在版本号前加 **`test-`** 前缀，便于与正式包区分。  
   - 示例（APK）：`test-1.5.10-48.apk`（由 `1.5.10+48` 将 `+` 换成 `-`，避免部分终端/脚本对 `+` 的解析问题）。  
   - 示例（IPA）：`test-1.5.10-48.ipa`。
4. **正式包**：可直接使用 `face-swap-1.5.10-48-release.apk` 或 `1.5.10-48-release.apk` 等包含版本号的命名，团队约定一种即可；**必须含版本号**。
5. 自动化脚本应从 `pubspec.yaml` **读取版本**生成输出文件名，避免手抄版本号。仓库内脚本：**`scripts/extract-pubspec-version.ps1`**。输出目录优先使用上表 **`F:\work\And\<项目名称>\test`** / **`and`**。

## GitHub Actions

- iOS IPA 等 CI 产物 artifact 名称应包含 `github.run_number` 或 `pubspec` 版本，避免与其它工作流混淆（具体以 workflow 内 `upload-artifact` `name` 为准）。
