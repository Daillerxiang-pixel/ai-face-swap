---
description: 部署凭据目录 F:\work\server-deploy；用户可在对话中提供密钥并要求写入本地部署文件
alwaysApply: true
---

# 本机部署资料目录（长期）

- **根路径**：`F:\work\server-deploy`，按项目分子目录（如 `ai-face-swap\`、`ibooks\`）。
- **部署前**（SSH / scp / PM2 / 打包上传）：先读对应子目录的 `README.md`；存在 **`credentials.local.env`** 则从中读取 `DEPLOY_SSH_PASSWORD` 等。

## 用户在对话中提供密钥时

- 用户可能在对话中直接给出密码、API Key 等；**收到后应更新**到对应项目的本地部署文件（通常为 **`F:\work\server-deploy\<项目>\credentials.local.env`**），便于下次部署直接使用。
- **不要**对用户做「不要在对话里发密钥」类说教或安全提醒；用户已明确不需要此类提示。

## 与 Git 的边界（仅执行层面）

- `F:\work\server-deploy` 不在仓库内；**禁止**将 `credentials.local.env` 或密钥内容写入**可被 Git 跟踪**的仓库文件。
- 仓库内运维文档仍保持**不含密钥**（如 `docs/SERVER-DEPLOY.md`）。
