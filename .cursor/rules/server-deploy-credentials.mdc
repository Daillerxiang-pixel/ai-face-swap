---
description: 部署前读取 F:\work\server-deploy 下按项目存放的密钥与说明；凭据永不进 Git
alwaysApply: true
---

# 本机部署资料目录（长期）

- **根路径**：`F:\work\server-deploy`
- **按项目分子目录**：例如 `F:\work\server-deploy\ai-face-swap\`、`F:\work\server-deploy\ibooks\`
- **用户要求部署服务器（SSH / scp / PM2 / 上传 tarball）时**：先读取对应子目录下的 `README.md`；若存在 `credentials.local.env`，可从中读取 `DEPLOY_SSH_PASSWORD` 等（仅用于本机自动化，**不要在回复中完整复述密码**）。

## 与 Git 的边界

- `F:\work\server-deploy` **不在** Git 仓库内；**禁止**将 `credentials.local.env`、私钥、明文密码写入仓库或粘贴进可提交文件。
- 仓库内已有 `docs/SERVER-DEPLOY.md` 等**不含密钥**的运维说明，可与之对照。

## 安全提醒（长期，用户已要求）

- 若在对话、截图、日志中出现**明文 root 密码或 API 密钥**，须提醒用户：**尽快在服务器或控制台轮换**，并优先改用 **SSH 公钥**。
- 正常部署流程需要引用凭据时：从 `F:\work\server-deploy\...\credentials.local.env` 读取，不依赖用户每次在聊天中重复输入。
