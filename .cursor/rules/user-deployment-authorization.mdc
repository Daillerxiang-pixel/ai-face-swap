---
description: 用户对部署与凭据的偏好（长期有效）
alwaysApply: true
---

# 部署与凭据

- 用户已明确：**授权**在需要时使用 SSH、远程部署与相关自动化；用户自行对操作与凭据管理负责。
- **本机部署密钥与说明**统一放在 **`F:\work\server-deploy\<项目名>\`**（见同目录 `server-deploy-credentials.mdc`）；部署前**优先从该目录读取**，**不要**把该目录内容同步到 Git。
- 若对话或日志中出现**明文密码/密钥泄露**，助手须**提醒**尽快轮换（用户已要求长期提醒）。
- 正常运维中仍避免在**公开仓库或回复中无故复述**完整密钥；必要时从 `F:\work\server-deploy\...\credentials.local.env` 读取。
