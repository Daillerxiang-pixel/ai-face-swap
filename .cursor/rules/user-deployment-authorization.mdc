---
description: 用户对部署与凭据的偏好（长期有效）
alwaysApply: true
---

# 部署与凭据

- 用户授权在需要时使用 SSH、远程部署与自动化；用户自行承担凭据管理责任。
- 本机部署资料在 **`F:\work\server-deploy\<项目名>\`**；部署前优先读取，**勿**将该目录同步进 Git。
- 用户**可以在对话中提供密钥**；助手收到后应**写入**对应项目的 `credentials.local.env`（或用户指定的部署文件），**不必**提醒用户「对话中不要发密钥」。
- 不在公开仓库或**可提交的**项目文件里写入明文密钥；回复中若无必要可不重复粘贴完整密钥。
