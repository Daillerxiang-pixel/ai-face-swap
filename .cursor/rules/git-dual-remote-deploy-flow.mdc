---
description: 单一代码库、测试先行、双远程推送（GitHub + Gitea）；部署从测试服验证后再上正式服
alwaysApply: true
---

# Git 与后端发布（长期原则）

## 何谓「一套代码」与「一致」

- **定义**：只 **维护一套** 后端业务代码（**一个** Git 仓库、通常一条主分支如 `main`），**没有**「测试服一套实现、正式服另一套实现」的分叉。
- **发布流向**：本地开发完成新功能 → **先** 推远端并 **部署到测试服** → 在测试服验证通过 → **再** 将 **同一提交** 部署到正式服。常态下测试服 **先于或等于** 正式服（新能力先上测试，再上生产）。
- **测试与正式仅在三处不同**：**部署环境**（机器、目录、端口、PM2 名、`server/.env` 等）、**域名**（对外入口与证书）、**数据**（各自独立数据库）。除此之外 **不** 另维护一套「测试专用业务代码」。

## 不存在「两套后端代码」

- **没有**「测试服专用后端仓库」与「正式服专用后端仓库」的分叉；**只有一个 Git 仓库、一条主分支**（通常为 `main`）。
- 测试机与正式机部署的是 **同一提交** 上的产物（同一 tar 排除规则：`prod_light_deploy.py` / `test_light_deploy.py`）。与上一节一致：差别是 **环境 / 域名 / 数据**，**不在仓库里维护两套业务代码**。

## 数据层：测试库 ≠ 正式库（与「同一套代码」无关）

- **两套数据库、两套数据**：测试服与正式服各用 **独立** 的库文件路径（见 `docs/SERVER-DEPLOY.md`），**不是**「一个库两边连」。
- **正式服库承载真实用户数据**：**禁止**随意清空、用测试库覆盖正式库、或在无备份与明确授权下对正式库做破坏性操作。测试环境可造数、可重置；正式环境变更须先备份、再按迁移/发布流程执行。
- **Schema** 随应用迁移与代码版本对齐；**业务数据内容** 不要求与测试「一致」，也**不要**假设两边数据相同。

## 发布顺序（与「一套代码」一致）

1. **本地开发**完成后，先 **部署到测试服**（`scripts/deploy/test_light_deploy.py` 或等价流程），在测试环境完成接口/客户端/业务流程验证。
2. **测试通过后**，再将 **已在测试服验证的同一提交** 部署到 **正式服**（`scripts/deploy/prod_light_deploy.py`）。  
   - 含义：**先测后发**；正式服应部署的即是测试服已验证过的那套提交，**不是**两条并行代码线。

## 双远程：GitHub + 私有化 Gitea

- **GitHub**：`git remote` 一般为 `github` → `https://github.com/Daillerxiang-pixel/ai-face-swap.git`
- **私有化 Gitea**（**仅此一份**，仅测试服）：HTTPS `https://test1.kanashortplay.com:8443/aihuantu/ai-face-swap.git`；SSH 须 **`Port 2222`**（Gitea 内置 SSH，勿用主机 22）；Web：`https://test1.kanashortplay.com:8443/aihuantu/ai-face-swap`。**正式服不部署 Gitea**；历史上复制到正式服的 Gitea 已全部销毁，**禁止**在正式机再装第二套 Gitea。本地 `git remote -v` 若仍指向已废弃地址，应改为上述测试服 URL。

**每次**需要把代码同步到远端时，应 **同时** 推送到两个远程（示例）：

```bash
git push github main
git push origin main
```

若某一远程暂时不可达，可稍后补推；**不要**长期只推一边导致 GitHub 与 Gitea 漂移。

## 打测试包或服务器更新时（强制捆绑同步）

以下任一操作发生时，**同一轮流程内**必须完成 **Git 双远程推送**，避免「包/线上已是新代码、远端仓库落后」：

- 打新的 **测试 APK**（或 IPA）；
- 执行 **测试服**（或正式服）**代码部署**（如 `test_light_deploy.py` / `prod_light_deploy.py` / 手工覆盖）。

**推荐顺序**（助手按此执行，除非用户明确改序）：

1. 工作区有改动则 **`git add` + `git commit`**（含 `app_temp/pubspec.yaml` 版本递增时一并提交）。
2. **`git push github <branch>`** 与 **`git push origin <branch>`** 均成功（或记下失败原因并提醒用户补推）。
3. 再 **部署服务器**（若本次包含服务端改动或用户要求发版）。
4. 再 **Flutter 构建测试包**（若本次要求打包）。

## 助手执行约定

- 完成与本仓库相关的提交后，**优先执行** `git push github <branch>` 与 `git push origin <branch>`（或用户当前分支），除非用户明确只要推一边。
- 用户仅说「打测试包」或「部署测试服」时，**默认仍执行双远程推送**（与上节一致），不单独只部署不推代码。
- 部署/排障时优先对照 **测试服 → 正式服** 的顺序，不假设「正式服单独改代码」。
- 涉及数据库时：区分 **测试库** 与 **正式库**；**勿**建议清空或覆盖正式用户数据，除非用户明确要求且已说明备份与影响范围。
