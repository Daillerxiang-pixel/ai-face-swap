name: net_layer
description: Reusable Dio HTTP client with interceptors, retry logic, and unified API response.
version: 0.1.0
publishable: false

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dio: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
