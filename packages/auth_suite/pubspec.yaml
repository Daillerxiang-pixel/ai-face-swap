name: auth_suite
description: Reusable authentication suite with token management and social sign-in support.
version: 0.1.0
publishable: false

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  sign_in_with_apple: ^6.1.0
  net_layer:
    path: ../net_layer

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
