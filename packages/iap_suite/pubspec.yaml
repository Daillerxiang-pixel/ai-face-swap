name: iap_suite
description: Reusable In-App Purchase suite with StoreKit/Google Play support and receipt verification.
version: 0.1.0
publishable: false

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  in_app_purchase: ^3.1.0
  net_layer:
    path: ../net_layer

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
